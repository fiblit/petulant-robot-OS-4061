#include "graphexec.h"

int main(int argc, char *argv[])
{
	/*Main structure
	fopen("in");
	while ((line = getLine()) != NULL)
		getNode(line);
	//buildGraph(Nodes); //not needed...
	buildRowList(nodes); //runs "BFS" naturally (it has to loop check to build)
	for ( int i = 0; i < rowList.size(); i++ ){
		fan ( rowList[i].size() )
		if childpid == 0
			exec (nodeij.prog)
		else
			while (r_wait > 0)
	}
	free rowlist
	*/
	return 0;
}
